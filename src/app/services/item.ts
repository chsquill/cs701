// represents an item in a column
export class Item {
  // unique id
  id:				number;
  // type of Item - NOTE, IMAGE, CLOCK, QUESTION
  type:     string;
  // data for item
  text:     string;
  // additional data for item
  pretext:  string;
}
